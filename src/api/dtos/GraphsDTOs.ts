export interface FromIntervalDto {
    startDate: string;
    endDate: string;
}

export interface ByDeviceDto {
    deviceModel: string;
    deviceBrand: string;
}

export interface ByRevenue {
    startDate: string;
    endDate: string;
}